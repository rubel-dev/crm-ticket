# Optional runtime configuration. The rule engine works without any external services.
APP_ENV=local
LOG_LEVEL=info

# Keep real secrets in your deployment environment, not in the repository.
# OPENAI_API_KEY=
